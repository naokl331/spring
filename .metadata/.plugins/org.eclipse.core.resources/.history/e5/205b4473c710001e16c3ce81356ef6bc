
 .btn_a {
   display: inline-block;
   padding: 5px 5px;
   background-color: tomato; /* ボタンの背景色 */
   color: #ffffff; /* ボタンの文字色 */
   text-decoration: none; /* 下線をなしにする */
   border-radius: 5px;
 }
  
 .btn_a:hover {
   background-color: crimson; /* ホバー時の背景色 */
   color: white; /* ホバー時の文字色 */
   /* その他のホバー時のスタイル */
 }
 
 .btn_b {
   display: inline-block;
   padding: 5px 5px;
   background-color: mediumseagreen; /* ボタンの背景色 */
   color: #ffffff; /* ボタンの文字色 */
   text-decoration: none; /* 下線をなしにする */
   border-radius: 5px;
 }
  
 .btn_b:hover {
   background-color: seagreen; /* ホバー時の背景色 */
   color: white; /* ホバー時の文字色 */
   /* その他のホバー時のスタイル */
 }

 .notCreated{color: tomato; font-weight: bold;}
 
 .Created{color: deepskyblue; font-weight: bold;}
 
 
 input { 
  margin-bottom: 10px; 
  background: white;
  border: none;
  outline: none;
  padding: 5px;
  font-size: 13px;
  color: black;
  text-shadow: 1px 1px 1px rgba(0,0,0,0.3);
  border: 1px solid rgba(0,0,0,0.3);
  border-radius: 4px;
  box-shadow: inset 0 -5px 45px rgba(100,100,100,0.2), 0 1px 1px rgba(255,255,255,0.2);
  -webkit-transition: box-shadow .5s ease;
  -moz-transition: box-shadow .5s ease;
  -o-transition: box-shadow .5s ease;
  -ms-transition: box-shadow .5s ease;
  transition: box-shadow .5s ease;
}
input:focus { box-shadow: inset 0 -5px 45px rgba(100,100,100,0.4), 0 1px 1px rgba(255,255,255,0.2); }

th,.Created,.notCreated{
	vertical-align: middle;
}

button a{
	color: white;
	text-decoration: none;
}
